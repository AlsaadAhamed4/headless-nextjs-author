/** @type {import('next').NextConfig} */
const nextConfig = {
    images :{
        domains: ["images.ctfassets.net"], // this is to use the images from contentfull to use the image component of next js, this is allowed list. 
    },
}

module.exports = nextConfig